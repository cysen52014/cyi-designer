import {
    CaEw
} from "@/api/gfilters";
const aa = val => {
    return {
        a: c
    } [val]
}
const ceg = async val => {
    const {
        errorCode,
        data
    } = await CaEw();
    if (errorCode === "0") {
        return data[val]
    };
}
}()